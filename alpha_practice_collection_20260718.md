# KNOW-20260718-005 Alpha Practice Review

Two high-signal forum discussions were added to `wqc`.

- AI can be used to propose semantic substitutions from a simple template to comparable fields or datasets, but those proposals require registry/type/operator/scope validation and human approval before any real action.
- Template-first dataset exploration may reduce search space, but public formulas are intentionally excluded until an approved template-library repository and import schema exist.

No real WQ action occurred. No formula, credentials, endpoint details, account data, submitted pool, PnL, or execution code is included.
