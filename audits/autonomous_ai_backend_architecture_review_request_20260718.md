Task: `AI-BACKEND-ARCH-AUDIT-20260718-001`

Return exactly one verdict: `PASS`, `FAIL`, or `BLOCKED`. Tie every non-PASS
finding to the supplied source evidence and identify the smallest required
correction.

Review the architecture package for the following conditions:

1. One authority chain remains intact from registry snapshot through result and audit.
2. AI, program, and human authority boundaries remain fail-closed.
3. Research Package V2 preserves its four-root-file contract.
4. Context compression preserves hard rules, current exact facts, source references, and omitted counts.
5. Provider and secret design permits unattended operation without exposing credentials.
6. Low-efficiency behavior creates bounded assistance instead of repeated similar candidate generation.
7. Current-system gaps are backed by static source evidence and source gaps remain `UNVERIFIED`.
8. The table conclusion is limited to five required new tables, with one provider-secret database table or an external Secret Store as mutually exclusive implementation choices; Campaign Event reuses or projects `MiningEvent` first.
9. Formal implementation cannot begin until a clean fact-source commit contains `20260718_0031`, exactly one Alembic Head is confirmed, and work starts in an independent worktree; absence of that evidence stops coding and reports a source gap.
10. The cumulative history has not been overwritten or deleted: `REG`, `CORE`, `SCHED`, `RESULT`, `RUNTIME-RECOVER`, `TPL`, `consultant_core`, `single_multi_allocation`, allocation decision, capacity policy, and scheduler recovery are present through the migration index and manifest.
11. The phased plan can be implemented without a second execution queue, result pullback, template library, factor store, scheduler bypass, or AI-owned authority path.
