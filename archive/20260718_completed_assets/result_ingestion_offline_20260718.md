# RESULT-20260718-001 Offline Result Ingestion Validation

## Outcome

PASS for the authorized offline scope. No WorldQuant request, real task, deployment, API Gate, budget, admission, credential, or frontend change was made.

## Audited overlap and ownership

- Execution Transport previously finalized requests, wrote `FeedbackRecord`, evaluated checks, projected Factor Center, and maintained batch aggregates in the same path.
- `SimulationBatch*` is the active transport parent/child state, while `MultiSimulation*` is the CORE canonical model. Both remain readable, but result interpretation and terminal aggregation now enter through Result Ingestion.
- Factor Center previously accepted several writers. Execution-result projection now has a canonical `normalized_result_id`; Factor Center remains a query/read projection.
- Research Exchange previously assembled execution feedback directly. Research Center now owns a bounded `ResearchFeedbackDelta`, and Exchange only exports those deltas by cursor.
- Research Package V1/V2 and Data Pullback retain packaging/archive behavior. They are not Result Ingestion entry points; Data Pullback remains cold archive only.

## Implemented data flow

`Execution Transport raw envelope -> ExecutionResultFact -> NormalizedExecutionResult -> parent/child aggregate -> FactorCheckRecord + CorrelationObservation -> Factor Center projection -> ResearchFeedbackDelta -> bounded Research Exchange export`

Migration `20260718_0030` adds the immutable raw-fact ledger, deterministic normalized result, bounded feedback delta, projection foreign keys, transport/CORE bridge keys, and a unique `(normalized_result_id, correlation_type, comparison_pool)` correlation boundary.

## Offline verification

- `python -m unittest backend/app/mining/result_ingestion_offline_test`: PASS, 10/10. Covers canonical payload hash, exact duplicate import, Invalid, Timeout, missing fields, partial Multi, successful Child with Parent error, timeout of only nonterminal Child, restart recovery, five independent correlation classes, and bounded Exchange feedback without PnL/raw result.
- `python -m unittest backend/app/mining/consultant_core_test`: PASS, 10/10.
- `python backend/app/mining/consultant_multi_transport_smoke_test.py`: PASS, 15/15.
- `python backend/app/mining/research_exchange_v2_test.py`: PASS.
- `python backend/app/mining/llm_research_memory_v2_smoke_test.py`: PASS, deterministic checks 12.
- `python backend/app/mining/research_center_smoke_test.py`: PASS, checks 10.
- Alembic graph: PASS, 30 revisions, one head `20260718_0030`.
- SQLAlchemy metadata create on in-memory SQLite: PASS, 75 tables.
- Data Pullback service-only smoke: PASS; it remains a cold archive.

## Known baseline exclusions

- `factor_center_smoke_test.py` still fails its pre-existing legacy external curve cache assertion (`has_curve is True`). The Result Ingestion projection tests pass; this task did not change legacy curve storage semantics.
- Full Research Package/Data Pullback route smoke remains blocked by the recovered baseline's missing `app.mining.agent_control_service` import from `app.main`. Service-level tests pass. This is not caused by migration `0030` and was not bypassed.

## Safety and review handoff

The local Git commit is the source-review artifact. A GPT review may receive only the sanitized governance/module/test summary generated by the established `wq-review` workflow. Full source, raw payloads, PnL curves, databases, credentials, logs, and production records must not be uploaded.
